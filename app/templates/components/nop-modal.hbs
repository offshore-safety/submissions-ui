{{#if visible}}
  {{#modal-dialog close='toggleModal'
                  targetAttachment="center"
                  translucentOverlay=true}}
    <h3>{{title}}</h3>
    <p>{{message}}</p>
    <button {{action 'toggleModal'}} class='primary'>Ok</button>
  {{/modal-dialog}}
{{/if}}
