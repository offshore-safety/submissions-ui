{{#if showRemove}}
  {{nop-remove action='remove'}}
{{/if}}

{{nop-uploader token=document.token
               name=document.name
               errors=document.errors
               errorKey='token'}}