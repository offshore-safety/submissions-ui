{{#if showRemove}}
  {{nop-remove action='remove'}}
{{/if}}
{{nop-input label='Additional email address'
            name='other-confirmation-email'
            type='email'
            value=email.email
            errors=email.errors
            errorKey='email'}}
