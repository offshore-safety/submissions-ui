{{#if readonly}}
  {{nop-titleholder-details titleholder=submission.titleholderDetails readonly=true}}

  {{nop-errors errors=errors}}
{{else}}
  {{nop-titleholder-details titleholder=submission.titleholderDetails}}
{{/if}}
