{{#if readonly}}
  {{#if comments}}
    {{nop-text-field label='Comments' value=comments}}
  {{else}}
    You have no comments to provide for this submission
  {{/if}}
{{else}}
  {{nop-textarea value=comments rows=10 label='Comments' hint=hint}}
{{/if}}
