<label for="{{concat elementId '-' name}}">{{label}}</label>
{{select-2 content=content 
           placeholder=placeholder
           multiple=multiple required=required value=value optionValuePath=optionValuePath id=(concat elementId '-' name)}}
<span class="highlight" />
{{#if errorMessage}}
  <div class="error-message">
    {{ errorMessage }}
  </div>
{{/if}}
